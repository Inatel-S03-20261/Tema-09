import { PokemonDTO } from '../types/PokemonDTO';

function CartaComponent() {
  const exibirCarta = (pokemon: PokemonDTO): void => {};
  const marcarComoConhecida = (pokemonId: number): void => {};

  return <div>Carta</div>;
}

export default CartaComponent;