export function usePokedex() {
  const carregarPokedex = (): void => {};
  const filtrarCartas = (): void => {};
  const consultarProgresso = (): void => {};

  return { carregarPokedex, filtrarCartas, consultarProgresso };
}